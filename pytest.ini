[pytest]
# The `nengo` package in this Anaconda env registers a pytest plugin that is
# broken under NumPy 2.x and crashes test collection. We don't use it here, so
# disable it. (Nothing to do with this project's code.)
addopts = -p no:nengo
testpaths = tests
